"""A fórmula de progressão aritmética é: Termo1 + 9 * Razao, onde Termo1 é o primeiro valor da progressão e
Razao é a diferença entre os termos.
Faça um algoritmo que resolva a fórmula acima."""

termo1 = float(input("Qual o primeiro termo? "))
razao = float(input("Qual a razao? "))

formula =termo1 + 9 * razao

print(formula)